---
'rn-motion-ui': patch
---

feat(Button): default to the pill shape

`Button`, `ElevatedButton`, `GlossyButton`, `StatefulButton`, and `IconButton`
now default `shape` to `pill` instead of `rounded`.
