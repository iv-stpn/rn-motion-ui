# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for rn-motion-ui (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-16 (commit d4ff50f9). Confidence: 100%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.
- **Recording a decision** you had to reason out: `repowise decision add --title T --decision D` records it without prompting and prints the id (`--format json` to parse it back). It lands `proposed`, for a person to confirm.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how/where/why question. Cite `confidence: "high"` or `grounding: "extracted"` directly; `degraded` means judge by `retrieval_quality`. `symbol_bodies` has live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 497 | **Lines:** 93094 | **Monorepo:** 5 packages | **Import cycles:** 6
rn-motion-ui is a multi-package typescript codebase of 497 files, split across 5 packages. Execution starts at storybook/demo/App.tsx, storybook/native/.rnstorybook/main.ts, storybook/web/.storybook/main.ts. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `packages/ui/src/moti` — packages/ui/src/moti/components · packages/ui/src/moti/core · packages/ui/src/moti/core/constants · packages/ui/src/moti/core/worklets ·…
- `packages/ui/src/components/menus` — packages/ui/src/components/menus/ActionFeedbackModal · packages/ui/src/components/menus/AdaptiveDropdown ·…
- `packages/ui/src/components/file-system` — packages/ui/src/components/file-system/FileIcon · packages/ui/src/components/file-system/FileSystem ·…
- `packages/ui/src/lib` — packages/ui/src/lib
**Language:** typescript | **Files:** 17 | **Public symbols:** 174 / 221
Covers the 17 source files in…
- `packages/ui/src/components/form` — packages/ui/src/components/form/Button · packages/ui/src/components/form/ButtonGroup · packages/ui/src/components/form/Checkbox ·…
- `packages/ui/src/components/gestures` — packages/ui/src/components/gestures · packages/ui/src/components/gestures/DragManager · packages/ui/src/components/gestures/Draggable ·…
- `packages/ui/src/components/display` — packages/ui/src/components/display/ActionSwap · packages/ui/src/components/display/AnimatedBadge ·…
- `packages/ui/src/components/menus/HoldMenu` — packages/ui/src/components/menus/HoldMenu
**Language:** typescript | **Files:** 26 | **Public symbols:** 84 / 152
Covers the 26 source…
- `packages/ui/src/hooks` — packages/ui/src/hooks
**Language:** typescript | **Files:** 20 | **Public symbols:** 37 / 72
Covers the 20 source files in…
- `packages/ui/src/moti/components` — packages/ui/src/moti/components · packages/ui/src/moti/presence
**Language:** typescript | **Files:** 7 | **Public symbols:** 10 /…

### Entry points
- `storybook/native/.rnstorybook/main.ts`
- `storybook/web/.storybook/main.ts`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `packages/ui/src/components/file-system/FileSystem/file-system.stories.tsx` — 7 bug fixes, last fix today (bug magnet); 16 commits/90d
- `packages/ui/src/components/menus/HoldMenu/hold-menu.stories.tsx` — 4 bug fixes, last fix today (bug magnet); 7 commits/90d
- `packages/ui/src/components/gestures/Draggable/draggable-session.ts` — 4 bug fixes, last fix 3 days ago (bug magnet); 6 commits/90d
- `packages/ui/src/components/menus/HoldContextMenu/hold-context-menu-trigger.tsx` — 3 bug fixes, last fix today; 7 commits/90d
- `packages/ui/src/components/gestures/Draggable/use-draggable-html5.ts` — 3 bug fixes, last fix 2 days ago; 7 commits/90d

### Code health
Three co-equal signals: defect risk 7.97/10 avg, hotspot health 6.23/10 (stable), worst `packages/ui/src/moti/presence/animate-presence.tsx` at 2.7/10 · maintainability 9.37/10 · performance risk 16 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `packages/ui/scripts/check-exports.mjs` — complex conditional (collectSourceFiles) — impact −2.5
- `packages/ui/src/components/gestures/SortableList/sortable-list.tsx` — change entropy — impact −2.3
- `packages/ui/src/lib/cn.ts` — churn risk — impact −2.2
- `packages/ui/src/theme/use-theme-color.ts` — untested hotspot — impact −2.0
- `packages/ui/src/lib/ease.ts` — untested hotspot — impact −2.0

### Commands
- Test: `npm run test`
- Lint: `npm run lint`
- Typecheck: `npm run typecheck`

<!-- REPOWISE:END -->
