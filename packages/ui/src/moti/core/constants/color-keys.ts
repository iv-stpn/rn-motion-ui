/**
 * Style property names that accept color values.
 *
 * Used internally by the animation worklets to distinguish color interpolations
 * (which go through `processColor`) from numeric ones.
 */
export const colorKeys = [
  'backgroundColor',
  'borderBottomColor',
  'borderColor',
  'borderEndColor',
  'borderLeftColor',
  'borderRightColor',
  'borderStartColor',
  'borderTopColor',
  'color',
];
