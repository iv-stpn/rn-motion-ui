// Type declaration for the side-effect CSS entry point.
// moduleResolution: "bundler" resolves this file on disk, so the wildcard
// `declare module '*.css'` in uniwind-env.d.ts doesn't apply — TypeScript
// needs an explicit sidecar here instead.
export {};
